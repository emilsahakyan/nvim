-- Extend LSP configuration
-- Add overrides for LSP server settings, the keys are the name of the server
return {
      -- pyright= {
      --   settings = {
        --   ...
      --     },
      --   },
      -- },
  }
